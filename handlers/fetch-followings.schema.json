{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Fetch followings request schema",
  "description": "Validates path parameters for the fetchFollowings handler",
  "required": ["platformName", "username"],
  "properties": {
    "platformName": {
      "type": "string",
      "title": "Platform name",
      "description": "Identifier of the platform to fetch followings from (e.g. MEDIUM)",
      "minLength": 1,
      "pattern": "^[a-zA-Z]+$"
    },
    "username": {
      "type": "string",
      "title": "Username",
      "description": "Username of the user whose followings will be fetched",
      "minLength": 1,
      "pattern": "^[a-zA-Z0-9]+$"
    }
  }
}
